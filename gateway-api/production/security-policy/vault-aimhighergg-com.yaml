apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: vault-aimhighergg-com
  namespace: vault
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: vault-aimhighergg-com
  authorization:
    defaultAction: Deny
    rules:
    - action: Allow
      principal:
        clientCIDRs:
        - 192.168.0.0/16
  basicAuth:
    users:
      name: basic-auth
      namespace: vault
