global:
  tlsDisable: false
injector:
  enabled: true
  extraEnvironmentVars:
    VAULT_ADDR: https://vault.vault.svc:8200
    VAULT_CACERT: /vault/tls/ca.crt
    VAULT_CLIENT_CERT: /vault/tls/tls.crt
    VAULT_CLIENT_KEY: /vault/tls/tls.key
  volumes:
    - name: vault-tls
      secret:
        secretName: vault-client-tls
  volumeMounts:
    - name: vault-tls
      mountPath: /vault/tls
      readOnly: true
  certs:
    secretName: vault-injector-tls
server:
  logLevel: "trace"
  dataStorage:
    enabled: true
    size: 10Gi
    mountPath: "/vault/data"
    storageClass: "longhorn"
    accessMode: ReadWriteOnce
    annotations: {}
    labels: {}
  extraEnvironmentVars:
    VAULT_CACERT: /vault/userconfig/vault-ha-tls/ca.crt
    VAULT_CLIENT_CERT: /vault/userconfig/vault-ha-tls/tls.crt
    VAULT_CLIENT_KEY: /vault/userconfig/vault-ha-tls/tls.key
    VAULT_TLS_REQUIRE_AND_VERIFY_CLIENT_CERT: "true"
  volumes:
    - name: userconfig-vault-ha-tls
      secret:
        defaultMode: 420
        secretName: vault-ha-tls
  volumeMounts:
    - mountPath: /vault/userconfig/vault-ha-tls
      name: userconfig-vault-ha-tls
      readOnly: true
  readinessProbe:
    enabled: false
  ha:
    enabled: true
    replicas: 3
    raft:
      enabled: true
      setNodeId: false
      config: |
        ui = true
        listener "tcp" {
          tls_require_and_verify_client_cert = "true"
          tls_disable = 0
          tls_cert_file = "/vault/userconfig/vault-ha-tls/tls.crt"
          tls_key_file  = "/vault/userconfig/vault-ha-tls/tls.key"
          tls_client_ca_file = "/vault/userconfig/vault-ha-tls/ca.crt"
          address = "[::]:8200"
          cluster_address = "[::]:8201"
          alpn_protocols = "h2"
          telemetry {
            unauthenticated_metrics_access = "true"
          }
        }
        storage "raft" {
          path = "/vault/data"
          tls_cert_file = "/vault/userconfig/vault-ha-tls/tls.crt"
          tls_key_file  = "/vault/userconfig/vault-ha-tls/tls.key"
          tls_client_ca_file = "/vault/userconfig/vault-ha-tls/ca.crt"
          retry_join {
            leader_api_addr = "https://vault-0.vault-internal:8200"
          }
          retry_join {
            leader_api_addr = "https://vault-1.vault-internal:8200"
          }
          retry_join {
            leader_api_addr = "https://vault-2.vault-internal:8200"
          }
        }
        service_registration "kubernetes" {}
ui:
  enabled: true
  serviceType: "ClusterIP"
  serviceNodePort: null
  externalPort: 8200
  targetPort: 8200
